# Soroban RPC endpoint (testnet)
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Deployed TrustPay escrow contract (testnet)
CONTRACT_ID=CCPOUXHJT3D7EKM44ASLS442QSNRF6IKCMIAI466FSCYMA6BWKDHHFR2

# Supabase project (service role key — server-side only, never expose to the frontend)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# How often to poll for new events, in milliseconds
POLL_INTERVAL_MS=10000
